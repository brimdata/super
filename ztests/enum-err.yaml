script: |
  ! super -i tsup -

inputs:
  - name: stdin
    data: |
      {e:"bang"::enum(foo,bar,baz)}

outputs:
  - name: stderr
    data: |
      stdio:stdin: symbol "bang" not a member of enum(foo,bar,baz)
