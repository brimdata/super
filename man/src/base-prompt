You are helping generate a Unix man page for a CLI tool called "super".

This is NOT a tutorial and NOT website documentation.
The target audience is experienced CLI users.

Constraints:
- Concise, reference-oriented language
- No storytelling, no marketing language
- No long explanations or background sections
- Structure must follow a traditional man page:
  NAME, SYNOPSIS, DESCRIPTION, OPTIONS, SUB-COMMANDS, OUTPUT, ERRORS, EXAMPLES, SEE ALSO
- Do not include an AUTHORS section
- Prefer definition lists for options
- Assume the user already knows what SuperDB is at a high level
- For sub-commands "db", "compile", and "dev", provide only a one-line description with a link to https://superdb.org/command/<sub-command>.html — do not enumerate sub-commands
- Output only the pandoc markdown document with no preamble, explanation, or commentary before or after it
- Base EXAMPLES on all entries in the "TL;DR" source material and reproduce the command lines exactly, character for character, while preserving their relative order
- Do not put any tables (e.g., the input/output format tables) verbatim in the output
- Include "db" in the list of output formats, but don't include any detailed text describing it
- Use fenced code blocks for all EXAMPLES
- Use plain fenced code blocks with no language specifier
- SEE ALSO should include exactly these three links:
  - SuperDB documentation: https://superdb.org
  - `super` command reference: https://superdb.org/command/super.html
  - SuperSQL language reference: https://superdb.org/super-sql/intro.html
- Hyperlinks should be shown as autolinks using angle brackets, e.g. <https://example.com>
- DESCRIPTION should consist of plain paragraphs only — no subsections or subheadings
- Definition list descriptions should start with a capital letter and end with a period
- OPTIONS subsections should be titled "Global Options", "Query Options", "Input Options", "Output Options"
- Entries in each OPTIONS subsection should be ordered alphabetically by flag name
- Flag names in OPTIONS definition list terms should use bold, not backticks
- There should never be repeated entries within any OPTIONS subsection (e.g, "-f" only appears once)
- In SYNOPSIS, do not use fenced code blocks; use bold for command names and flags, italics for placeholders
- Operator, aggregate function, and function names should appear bold in the text
- References to the command name "super" in should appear bold
- In definition lists, use exactly three spaces after the colon (`:   `)

Your task is to distill the essential reference information from the provided command help summary and Markdown files.

Below are the source documents generated by "super -h" and a concatenation of all relevant docs from the detailed mdbook docs that are published on the web, shown here in .md format. Reduce them to what belongs in a single global man page.
Do not invent features. Do not add explanations not present in the sources.
Read the material located in the provided material to get the detail of each option, commands and sub-commands.
Include all of the "defaults" shown in the "super -h" output.
For the "compile", "db", and "dev" sub-commands, only small excerpts from the mdbook docs are provided because these are rarely used.

Produce a Pandoc-compatible Markdown suitable for conversion to a man page.
Include a Pandoc title block as the first three bare lines of the file with no preceding blank line and no code fence, using exactly this text:
% super(1)
%
% DATE_PLACEHOLDER
