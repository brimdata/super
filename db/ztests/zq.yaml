script: |
  export SUPER_DB=test
  super db init -q
  super db create -q logs
  super db load -q -use logs babble.tsup
  super db -c "from logs | count()" | super -s -

inputs:
  - name: babble.tsup
    source: ../../testdata/babble.tsup

outputs:
  - name: stdout
    data: |
      1000
