script: |
  export SUPER_DB=test
  super db init -q
  super db create -q test
  super db load -use test in.tsup

inputs:
  - name: in.tsup
    data: |
      {x:1}

outputs:
  - name: stdout
    regexp: |
      \w{27} committed
