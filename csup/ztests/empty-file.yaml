skip: "fails with '/dev/null: Super Columnar requires a seekable input'"

script: |
  super -i csup /dev/null

outputs:
  - name: stdout
    data: ""
  - name: stderr
    data: ""

---

script: |
  super -o test.csup -c 'values false | where this'

outputs:
  - name: test.csup
    data: ""
