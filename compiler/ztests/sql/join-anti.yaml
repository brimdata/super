skip: need to handle missing differently in SQL

spq: |
  select *
  from (values {aid:1}, {aid:2}, {aid:3})
  anti join (values {bid:2}) on aid == bid
  order by aid

output: |
  {aid:1,bid?:none::int64}
  {aid:3,bid?:none::int64}
