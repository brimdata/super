script: |
  super -s -c 'select a from "a.tsup"'
  echo ===
  super -s -c 'select l.a,r.b from "a.tsup" l join "b.tsup" r on l.c==r.c'
  echo ===
  super -s -c 'select l.a,m.s from "a.tsup" l join "messy.tsup" m on l.c==m.s'

inputs:
  - name: a.tsup
    data: |
      {a:11,c:1}
      {a:12,c:2}
      {a:13,c:3}
      {a:14,c:4}
  - name: b.tsup
    data: |
      {b:[1,2],c:2}
      {b:[3,4,5],c:3}
      {b:[6,7],c:5}
  - name: messy.tsup
    data: |
      {x:1,s:"foo"}
      {x:2,s:"bar"}
      {x:3,s:4}

outputs:
  - name: stdout
    data: |
      {a:11}
      {a:12}
      {a:13}
      {a:14}
      ===
      {a:12,b:[1,2]}
      {a:13,b:[3,4,5]}
      ===
      {a:14,s:4}
