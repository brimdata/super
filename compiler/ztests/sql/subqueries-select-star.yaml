script: |
  super -s -c 'select id from ( select * from (values (1)) x(id) )'
  echo // ===
  # Ensure id can be selected when subquery has select * on a dynamic input.
  super -s -c 'select id from ( select * from in.tsup )'

inputs:
  - name: in.tsup
    data: |
      {id:1}

outputs:
  - name: stdout
    data: |
      {id:1}
      // ===
      {id:1}
