script: |
  mkdir -p path/to
  super -o path/to/foo.parquet -f parquet foo.sup
  ! super -s -c 'select foo.id from path/to/foo.parquet'

inputs:
  - name: foo.sup
    data: &data |
      {id:1}
      {id:2}

outputs:
  - name: stderr
    data: |
      column "foo": does not exist at line 1, column 8:
      select foo.id from path/to/foo.parquet
             ~~~~~~
