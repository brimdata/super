script: |
  super -s -I query.spq

inputs:
  - name: query.spq
    data: |
      let slurp = (from in.tsup | count())
      values {x:slurp}, {y:slurp}
  - name: in.tsup
    data: |
      1
      2
      3

outputs:
  - name: stdout
    data: |
      {x:3}
      {y:3}
