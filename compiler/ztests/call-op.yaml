script: |
  super -s -c "op foo: (values 10) values {x:(call foo)}" in.tsup
  echo ===
  super -s -c "op foo: (values 10) values {x:(this.foo)}" in.tsup
  echo ===
  super -s -c "let foo = (values 10) values {x:(foo),y:foo,z:[foo]}" in.tsup

inputs:
  - name: in.tsup
    data: |
      {foo:1}

outputs:
  - name: stdout
    data: |
      {x:10}
      ===
      {x:1}
      ===
      {x:10,y:10,z:[10]}
