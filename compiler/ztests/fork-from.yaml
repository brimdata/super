script: |
  super -s -I query.spq

inputs:
  - name: query.spq
    data: |
      fork
        ( from a.tsup )
        ( from b.tsup )
      | sort a
  - name: a.tsup
    data: |
      {a:1}
  - name: b.tsup
    data: |
      {a:2}

outputs:
  - name: stdout
    data: |
      {a:1}
      {a:2}
