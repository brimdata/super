script: |
  super compile -C -dag 'fn h(e): ( has(e) ) h(this)'

outputs:
  - name: stdout
    data: |
      fn 0/h(e): (
        quiet(e) != none
      )
      null
      | where 0(this)
      | output main
