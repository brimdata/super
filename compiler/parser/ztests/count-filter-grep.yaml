script: |
  super -s -c 'count() filter ( grep("foo", this) )' in.tsup
  echo ===
  super -s -c "?foo | count()" in.tsup

inputs:
  - name: in.tsup
    data: |
      {_path:"conn"}
      {_path:"foo"}
      {_path:"conn"}
      {a:["foo","bar"]}
      {a:["bar"]}

outputs:
  - name: stdout
    data: |
      2
      ===
      2
