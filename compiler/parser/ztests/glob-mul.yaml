script: |
  super -dynamic -s -c "? a*b" in.tsup
  echo ===
  super -dynamic -s -c "? a*b=s" in.tsup

inputs:
  - name: in.tsup
    data: |
      {s:"axb"}
      {s:6,a:2,b:3}
      {s:7,a:2,b:3}

outputs:
  - name: stdout
    data: |
      {s:"axb"}
      ===
      {s:6,a:2,b:3}
