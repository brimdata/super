script: |
  super -s -c "_path == 'conn' | count()" in.tsup
  echo ===
  super -s -c "count() filter ( _path == 'conn' )" in.tsup

inputs:
  - name: in.tsup
    data: |
      {_path:"conn"}
      {_path:"dns"}
      {_path:"conn"}

outputs:
  - name: stdout
    data: |
      2
      ===
      2
