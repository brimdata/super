spq: |
  fn u(s): upper(s)
  const foo=u('bar')
  values foo

output: |
  "BAR"

---

# Test const that calls to a function with a dynamic result fails.
spq: |
  fn u(s): (from f"{s}")
  const foo=u('file.sup')
  values foo

error: |
  cannot read data in constant expression at line 1, column 16:
  fn u(s): (from f"{s}")
                 ~~~~~~
