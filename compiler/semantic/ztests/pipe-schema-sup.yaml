script: |
  super -s -c "select x from (from x.tsup | values {x}) join (from y.tsup | values {y}) on x=y"

inputs:
  - name: x.tsup
    data: |
      {x:1}
  - name: y.tsup
    data: |
      {y:1}
      {y:2}

outputs:
  - name: stdout
    data: |
      {x:1}
