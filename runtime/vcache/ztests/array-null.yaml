script: |
  super -f csup -o test.csup in.tsup
  super dev vector copy -s test.csup

inputs:
  - name: in.tsup
    data: &input |
      {a:[1,2]}
      null
      {a:[3]}

outputs:
  - name: stdout
    data: *input
