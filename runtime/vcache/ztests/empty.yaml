script: |
  super -f csup -o test.csup in.tsup
  super dev vector copy -s test.csup

inputs:
  - name: in.tsup
    data: &input |
      {}
      []
      map{}
      set[]

outputs:
  - name: stdout
    data: *input
