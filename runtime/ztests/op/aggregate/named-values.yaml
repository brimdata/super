spq: count(), sum(this), min(this), max(this), avg(this), collect(this), any(this), dcount(this)

flags: -pretty 4

input: | 
  type named=int64
  1::named
  2::named

output: |
  type named = int64
  {
      count: 2,
      sum: 3,
      min: 1,
      max: 2,
      avg: 1.5,
      collect: [
          1::named,
          2::named
      ],
      any: 1::named,
      dcount: 2
  }
