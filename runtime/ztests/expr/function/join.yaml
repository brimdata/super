spq: |
  values join(this, "->")

input: |
  ["foo","bar","baz"]
  ["foo"]
  null
  [null]
  []::[string]
  [""]
  "not an array"
  fusion([fusion("foo"::(int64|string),<string>),fusion("bar"::(int64|string),<string>)],<[string]>)
  fusion([fusion(1::(int64|string),<int64>)],<[int64]>)


output: |
  "foo->bar->baz"
  "foo"
  null
  error({message:"join: array of string arg required",on:[null]})
  ""
  ""
  error({message:"join: array of string arg required",on:"not an array"})
  "foo->bar"
  error({message:"join: array of string arg required",on:[1]})

---

# Single arg
spq: |
  join(this)

input: |
  ["f",".bar.com"]

output: |
  "f.bar.com"

---

# Bad separator arg
spq: join(["foo","bar"], this)

input: |
  null
  1

output: |
  null
  error({message:"join: separator must be string",on:1})
