spq: fuse -complete | defuse(this)

vector: true

input: &input |
  [1,2]
  ["foo"]
  [null]

output: *input

---

spq: fuse -complete | defuse(this)

input: &input |
  {x:1}
  {x:2,y?:3}

output: *input

