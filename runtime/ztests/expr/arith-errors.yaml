spq: s/0

input: |
  {}
  {s:error("other")}
  {s:1}

output: |
  error({message:"/: error value encountered",on:error({message:"no such field s",on:{}})})
  error({message:"/: error value encountered",on:error("other")})
  error("divide by zero")

---

spq: a+1

input: |
  {a?:none::int64}

output: |
  error("+: illegal none value")

---

spq: this+1

input: |
  none

output: |
  error("+: illegal none value")
