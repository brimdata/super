spq: s/0

input: |
  {}
  {s:error("other")}
  {s:1}

output: |
  error({message:"error on left-hand side of /",on:error({message:"no such field s",on:{}})})
  error({message:"error on left-hand side of /",on:error("other")})
  error("divide by zero")
