spq: values case x when 1 then "foo" when 2 then "bar" else {y:12} end

input-flags: -dynamic

# If we are following SQL then this should have a test with a mismatched typed
# value that gets an error, however we can't do this because mismatched
# comparisons do not currently return errors.
input: |
  {x:1}
  {x:2,y:3}
  {x:3}
  1

output: |
  "foo"
  "bar"
  {y:12}
  error({message:"==: error value encountered",on:error({message:"'.': applied to non-record",on:1})})

---

spq: values case x when 1 then "foo" when 2 then "bar" else {y:12} end

# If we are following SQL then this should have a test with a mismatched typed
# value that gets an error, however we can't do this because mismatched
# comparisons do not currently return errors.
input: |
  {x:1}
  {x:2,y:3}
  {x:3}
  1

error: |
  no such field "x" at line 1, column 13:
  values case x when 1 then "foo" when 2 then "bar" else {y:12} end

# ^^^ improve this error message with offending type and clarify that some cases work
