script: |
  super -s -c 'values exists(select * from (values ("foo"), ("bar")))'
  echo // ===
  super -s -c 'values exists(values null)'
  echo // ===
  super -s -c 'values exists(values "foo", "bar" | where this == "baz")'

vector: true

outputs:
  - name: stdout
    data: |
      true
      // ===
      true
      // ===
      false
