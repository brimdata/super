spq: cast(this, "named")

vector: true

input: |
  {x:1}
  {x:2}
  "foo"
  "bar"
  "baz"::=a
  error("missing")
  error("foo")

output: |
  {x:1}::=named
  {x:2}::=named
  "foo"::=named
  "bar"::=named
  "baz"::=named
  error("missing")
  error("foo")

---

# Tests issue in vector runtime that caused panic when casting to a named
# primitive.
spq: cast(80, <port=uint16>)

vector: true

output: |
  80::(port=uint16)
