skip: incorrect output

script: |
  super -aggmem 1B -s -c 'collect(this)' a.tsup
  ! super -aggmem 0 a.tsup

inputs:
  - name: a.tsup
    data: |
      {a:1}

outputs:
  - name: stdout
    data: |
      null
  - name: stderr
    data: |
      aggmem value must be greater than zero
