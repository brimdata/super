script: |
  super -j in.tsup
  ! super -j -f tsup in.tsup
  ! super -j -s in.tsup
  ! super -j -S in.tsup

inputs:
  - name: in.tsup
    data: |
      {a:1}

outputs:
  - name: stdout
    data: |
      {"a":1}
  - name: stderr
    data: |
      cannot use -j or -J with -f, -s, or -S
      cannot use -j or -J with -f, -s, or -S
      cannot use -j or -J with -f, -s, or -S
