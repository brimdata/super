script: |
  super -s -I countfile.spq -c countfile

inputs:
  - name: countfile.spq
    data: |
      op countfile: (
        from test.tsup | count()
      )
  - name: test.tsup
    data: '{} {} {} {}'

outputs:
  - name: stdout
    data: |
      4
