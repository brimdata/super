script: |
  super -s -c 'from a.tsup'
  super -s -I query.spq

inputs:
  - name: a.tsup
    data: |
      {a:1}
  - name: query.spq
    data: |
      from a.tsup

outputs:
  - name: stdout
    data: |
      {a:1}
      {a:1}
