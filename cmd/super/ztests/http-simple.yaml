script: |
  . http.bash
  super -s -c 'count()' $http_base_url/babble.tsup

inputs:
  - name: http.bash
  - name: babble.tsup
    source: ../../../testdata/babble.tsup

outputs:
  - name: stdout
    data: |
      1000
