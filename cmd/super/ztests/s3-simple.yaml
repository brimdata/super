script: |
  source minio.sh
  mv babble.tsup ./data/bucket
  super -s -c "count()" s3://bucket/babble.tsup

inputs:
  - name: babble.tsup
    source: ../../../testdata/babble.tsup
  - name: minio.sh
    source: ../../../testdata/minio.sh

outputs:
  - name: stdout
    data: |
      1000
