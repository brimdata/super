spq: defuse(this)

flags: -i json -f json

input: &input |
  {"b":true,"a":[{"x":1}]}
  {"a":[]}

output: *input

---

spq: defuse(this)

flags: -i json -f json

input: &input |
  {"a":[]}
  {"b":true,"a":[{"x":1}]}

output: *input

---

spq: defuse(this)

flags: -i json -f json

input: &input |
  ["foo"]
  []
  []
  ["bar"]

output: *input
