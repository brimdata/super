script: |
  super -f bsup -i tsup - | super -i bsup -s -

inputs:
  - name: stdin
    data: |
      1
      "hello"
      error("foo")

outputs:
  - name: stdout
    data: |
      1
      "hello"
      error("foo")
