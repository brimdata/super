script: |
 super - | super -s -o out.tsup long.tsup
 super - | super -s -o out.tsup short.tsup
 super -s out.tsup

inputs:
  - name: short.tsup
    data: |
      {a:"hello"}
  - name: long.tsup
    data: |
      {a:"hello"}
      {a:"there"}

outputs:
  - name: stdout
    data: |
      {a:"hello"}
