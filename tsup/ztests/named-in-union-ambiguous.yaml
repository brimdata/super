script: |
  super -s -c "typeof(this)" in.tsup
  echo ===
  super -s -c 'type foo=int64 typeof([1::foo,"bar"])'

inputs:
  - name: in.tsup
    data: |
      type foo=int64
      [1::foo,"bar"]

outputs:
  - name: stdout
    data: |
      type foo=int64
      <[foo|string]>
      ===
      type foo=int64
      <[foo|string]>
