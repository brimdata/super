script: |
  super -s in.tsup

inputs:
  - name: in.tsup
    data: |
      []
      [1]
      {version:[]::[null]}
      {version:[1]}

outputs:
  - name: stdout
    data: |
      []
      [1]
      {version:[]::[null]}
      {version:[1]}
